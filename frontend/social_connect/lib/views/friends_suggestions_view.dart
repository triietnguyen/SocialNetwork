import 'package:flutter/material.dart';

class FriendsSuggestionsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Friends Suggestions'),
      ),
      body: Center(
        child: Text('Friends Suggestions Content'),
      ),
    );
  }
}
